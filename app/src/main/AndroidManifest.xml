<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ac.neec.myapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRIGHT_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/wr_touka"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/SingleActionBarTheme" />
        <activity
            android:name=".SearchActivity"
            android:label="@string/title_activity_search"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TimeLineActivity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SingleViewActivity_Timeline"
            android:label="@string/title_activity_fav__search"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name=".SingleViewActivity_Search"
            android:label="@string/title_activity_fav__search"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name=".MypageActivity"
            android:label="@string/title_activity_mypage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OtherpageActivity"
            android:label="@string/title_activity_mypage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DressCodeActivity" />
        <activity
            android:name=".FavEditActivity"
            android:label="@string/title_activity_fav_edit"
            android:theme="@style/NoActionBarTheme" />
        <activity android:name=".PicAppDateActivity" />
        <activity android:name=".PicAppDateActivity2" />
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".LoginNewActivity"></activity>
    </application>

</manifest>